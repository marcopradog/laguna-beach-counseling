---
layout: layouts/base.njk
title: null
description: A warm, experienced Laguna Beach psychotherapy practice for individuals, couples, and families.
---
{# Foundation placeholder only. Real homepage content is built in a later phase
   (see SITE_ARCHITECTURE.md). This exists so the shell renders and / resolves. #}
<section class="container page-shell">
  <p style="font-family: var(--font-sans); letter-spacing: 0.2em; text-transform: uppercase; font-size: 0.8rem; color: var(--color-sun-deep);">{{ site.shortName }}</p>
  <h1>{{ site.tagline }}</h1>
  <p style="max-width: 55ch;">The new Laguna Beach Counseling website is being built. The foundation, brand, and shared components are in place. Page content is on the way.</p>
  <p><a class="btn btn-primary" href="{{ site.booking.getStartedPath }}">{{ site.booking.callLabel }}</a></p>
</section>
